[project]
name = "trace-grounded-repair"
version = "0.1.0"
description = "Trace digest prototype for LLM-oriented program repair experiments."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
tgr-smoketest = "tgr.harness.smoketest:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
